# Local / assistant notes (not shipped)
CLAUDE.md
REVIEW.md
/docs/

# Release artifacts (scripts/build-release.sh)
/dist/

# Local WordPress.org SVN working copy (deploy via scripts/deploy-wordpress-org-svn.sh)
admin-filters-for-memberpress-svn/

# Node (asset minification)
node_modules/

# Composer & PHPUnit
/vendor/
composer.phar
.phpunit.result.cache

# OS / editor noise
.DS_Store
